(version dev)

(build
 (all_platforms
  ((action
    (progn
     (run find . -path "*/test/*/dune" -delete)
     (run dune build -p matrix @install))))))

(depends
 (all_platforms
  (ocaml dune)))

(source
 (fetch
  (url git+https://github.com/tmattio/mosaic.git)))

(dev)
